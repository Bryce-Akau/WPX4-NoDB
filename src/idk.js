// // first search bar for genre type (yasss, meh, relax, old sku)
// // search for yasss to return yasss music if not do nothing then do the same for the rest.

// // type

// getType () {
//     let allSongs = this.state.song.slice ();
//     let filteredSongs = allSongs.filter ( (e) => {
//         if (e.type === this.state.type) {
//             return true;
//         }
//     })
//     return filteredSongs;
// }








// getSolo () {
//     let songs = this.getSongs ();

// };
//     return(
//         <select onChange={(e) => this.updateSoloStatus(e.value)} value={this.state.solo}>
//             <option value={true}>I am alone</option>
//             <option value={false}>I'm with my homies</option>
//         </select>
//         {songs}
//     );